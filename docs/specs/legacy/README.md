# Legacy Specs Snapshot

This directory stores a snapshot of pre-normalization spec files copied from `docs/spec/`.

Active specs are now maintained under `docs/specs/`.

Do not add new requirements to legacy docs.
